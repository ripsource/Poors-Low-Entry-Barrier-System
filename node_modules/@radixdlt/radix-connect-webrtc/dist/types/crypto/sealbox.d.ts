/// <reference types="node" />
import { Buffer } from 'buffer';
import { Result } from 'neverthrow';
export type SealedBoxProps = {
    ciphertext: Buffer;
    iv: Buffer;
    authTag: Buffer;
    combined: Buffer;
    ciphertextAndAuthTag: Buffer;
};
export declare const transformBufferToSealbox: (buffer: Buffer) => Result<SealedBoxProps, Error>;
