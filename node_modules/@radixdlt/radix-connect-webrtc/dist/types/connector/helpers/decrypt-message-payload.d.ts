/// <reference types="node" />
import type { DataTypes } from '@radixdlt/radix-connect-schemas';
import type { ResultAsync } from 'neverthrow';
export declare const decryptMessagePayload: <T = {
    sdp: string;
} | {
    candidate: string;
    sdpMid: string;
    sdpMLineIndex: number;
} | {
    sdp: string;
} | {
    candidate: string;
    sdpMid: string;
    sdpMLineIndex: number;
}[]>(message: DataTypes, encryptionKey: Buffer) => ResultAsync<T, Error>;
