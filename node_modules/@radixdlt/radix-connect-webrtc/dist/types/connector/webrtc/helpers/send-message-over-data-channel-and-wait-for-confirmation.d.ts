import type { ChunkedMessageType, MessageErrorReasons } from '../../../_types';
import { ResultAsync } from 'neverthrow';
import type { Subject } from 'rxjs';
export declare const sendMessageOverDataChannelAndWaitForConfirmation: (input: {
    message: Record<string, any>;
    sendMessageOverDataChannelSubject: Subject<string>;
    onDataChannelMessageSubject: Subject<ChunkedMessageType>;
    messageEventCallback: (event: 'messageSent') => void;
    timeout?: number | undefined;
    chunkSize: number;
}) => ResultAsync<undefined, {
    reason: MessageErrorReasons;
}>;
